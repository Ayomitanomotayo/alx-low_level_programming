#include "main.h"
/**
 * _abs - computing absolute value of an integer
 * @n: interger checked for
 * Return: Alway -num or num
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
