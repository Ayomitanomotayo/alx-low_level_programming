#include <stdio.h>

/**
 * main - A c program to print lower case alphabet a - z
 * Return: Always (success)
 */

int main(void)

{
	char C;

	for (C = 'a'; C  <= 'z'; C++)
	putchar(C);
	putchar('\n');
	return  (0);

}
