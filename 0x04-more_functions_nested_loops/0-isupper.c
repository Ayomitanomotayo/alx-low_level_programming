#include "main.h"

/**
 * _isupper - checking for upper case letters
 *@c: letter to check for
 *
 * Return: Always succes
 */

int _isupper(int c)
{
	if (c == 'A')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
